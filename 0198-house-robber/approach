In this problem,

always track the current max value

amount[0] = nums[0]
amount[1] = max(nums[0], nums[1])

amount[i] = max(nums[i] + amount[i-2], amount[i-1])

new amount would be derived by comparing with current amount
